apiVersion: v1
kind: Service
metadata:
  name: eventix-frontend
  namespace: default
  labels:
    app: eventix-frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: eventix-frontend
